# Chaitanya Srivastava Portfolio

GitHub Pages-ready multi-page case study portfolio.

## Run locally
npm install
npm run dev

## Deploy
Push to main. GitHub Actions will build and deploy the static export.

## Important
Set the repo name in `NEXT_PUBLIC_REPO_NAME` or rename the repository to match `next.config.ts`.
